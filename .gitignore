node_modules/

# Biometric working data — face descriptors and landmarks NEVER leave this machine.
.face-cache/

# Full-resolution originals (~446 MB). Only the web derivatives in
# assets/gallery/ are committed and deployed. See README.md.
assets/images/t-*.jpg
!assets/images/t-58-preview.jpg

# OS / editor noise
Thumbs.db
desktop.ini
.DS_Store
.vscode/
assets/images/*.zip

# Pre-apply safety snapshots from tools/review-server.mjs (npm run review).
# Each is fully superseded by the next one; git itself is the real history
# for these files, so these are local rollback insurance, not an archive.
face-index/backups/
